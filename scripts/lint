#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> Running Ruff format..."
uv run ruff format .

echo "==> Running Ruff check..."
uv run ruff check . --fix

echo "==> Linting completed!"
